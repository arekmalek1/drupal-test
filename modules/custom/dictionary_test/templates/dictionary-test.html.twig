{#
/**
 * @file
 * Default theme implementation to present a dictionary test entity.
 *
 * This template is used when viewing a registered dictionary test's page,
 * e.g., /admin/content/dictionary-test)/123. 123 being the dictionary test's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_dictionary_test()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
